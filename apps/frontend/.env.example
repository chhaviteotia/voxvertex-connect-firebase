# Frontend env – copy to .env in this directory for local overrides.
# In production (Vercel/AWS), set these in the dashboard.

# API base URL. Leave empty when using dev proxy (Vite proxies /api to backend).
# Set in production if API is on another origin, e.g. https://api.yourdomain.com
# VITE_API_URL=
